import average from "./package/average";
import { dateFormat } from "./package/date";
import { debounce } from './package/debounce';
import { addClass, removeClass, hasClass } from './package/dom'
import encodeParam from "./package/encodeParam";
import { preloadScript, loadScript } from "./package/loadScript";
import { typeOf } from './package/objects'
import { sleep } from './package/sleep'
import { randomStr } from './package/strings'
import { throttle } from './package/throttle'



export { average,dateFormat,debounce, addClass, removeClass, hasClass, encodeParam, preloadScript,
     loadScript, typeOf, sleep, randomStr, throttle };
